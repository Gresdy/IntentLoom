# GitHub secret scanning configuration for Gresdy/IntentLoom.
#
# Keep repository-wide secret scanning enabled. Public OAuth client values,
# if ever added back to the source, must be reviewed and allowlisted by their
# exact rule rather than excluding an entire source file from scanning.
